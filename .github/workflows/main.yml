name: PHP Deploy Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy IT-Portfolio
        run: |
          cd /var/www/IT-Portfolio
          git fetch origin
          git reset --hard origin/main
